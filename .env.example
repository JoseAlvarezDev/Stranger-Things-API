# Production environment
NODE_ENV=production
PORT=3000

# Optional: If you want to restrict CORS in production
# CORS_ORIGIN=https://your-frontend-domain.com

# Optional: API key for admin functions (future)
# ADMIN_API_KEY=your-secret-key
